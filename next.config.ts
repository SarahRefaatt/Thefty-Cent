import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "bxbmnzsgd3bynspk.public.blob.vercel-storage.com",
      // Add other domains if needed
    ],
  },
};

export default nextConfig;
